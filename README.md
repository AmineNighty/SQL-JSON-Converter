# SQL-JSON-Converter

**SQL-JSON-Converter** est une application Python (Tkinter) permettant de convertir facilement des données entre les formats **SQL → JSON** et **JSON → SQL**.  
Elle offre une interface simple, intuitive et rapide pour tous ceux qui manipulent régulièrement des structures SQL ou JSON.

---

## 🚀 Fonctionnalités

- Conversion **SQL vers JSON**
- Conversion **JSON vers SQL**
- Détection automatique du type d’entrée
- Interface graphique moderne avec Tkinter
- Icônes sur les boutons pour une meilleure UX
- Gestion des erreurs (input vide, format incorrect, etc.)

---

## 🛠 Technologies utilisées

- **Python 3.x**
- **Tkinter** (UI)
- **json** (module standard)
- **re** (regex)

---

## 📦 Installation

1. Clonez le dépôt :
```bash
git clone https://github.com/AmineNighty/SQL-JSON-Converter.git
